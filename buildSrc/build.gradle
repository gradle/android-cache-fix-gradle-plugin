plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        signingPlugin {
            id = 'org.gradle.android.signing'
            implementationClass = 'SigningPlugin'
        }
    }
}
