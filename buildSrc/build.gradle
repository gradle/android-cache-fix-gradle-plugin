plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        signingPlugin {
            id = 'org.gradle.android.cache-fix.signing'
            implementationClass = 'SigningPlugin'
        }
    }
}
